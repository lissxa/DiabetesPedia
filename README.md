# Machine Learning

Ini adalah folder untuk source code machine learning dari aplikasi DiabetesPedia.

cara run model api

# install dulu semua package/depedensi nya

pip install -r requirements.txt

# run api nya

uvicorn app:app --reload
